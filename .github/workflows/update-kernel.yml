name: Update kernel

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  bump:
    name: Bump stable kernel pin
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          token: ${{ secrets.WORKFLOW_UPDATE_TOKEN }}
      - name: Discover latest stable tag
        id: discover
        shell: bash
        run: |
          set -eou pipefail
          wf=.github/workflows/build-kernel.yml
          repo=$(grep -oP "kernel-repo: '\K[^']+" "$wf" | head -1)
          current=$(grep -oP "rev: 'v\K[^']+" "$wf" | head -1)
          series=${current%.*}
          latest=$(git ls-remote --tags --refs \
                     "https://github.com/${repo}" "v${series}.*" \
                   | awk -F/ '{print $NF}' \
                   | sort -V \
                   | tail -1)
          latest=${latest#v}
          echo "current=${current}" >> "$GITHUB_OUTPUT"
          echo "latest=${latest}" >> "$GITHUB_OUTPUT"
          echo "Current: v${current}; latest: v${latest}"
      - name: Apply bump
        if: steps.discover.outputs.latest != steps.discover.outputs.current
        shell: bash
        run: |
          set -eou pipefail
          old='${{ steps.discover.outputs.current }}'
          new='${{ steps.discover.outputs.latest }}'
          sed -i "s|rev: 'v${old}'|rev: 'v${new}'|" \
            .github/workflows/build-kernel.yml \
            .github/workflows/test.yml
      - name: Open PR
        if: steps.discover.outputs.latest != steps.discover.outputs.current
        env:
          GH_TOKEN: ${{ secrets.WORKFLOW_UPDATE_TOKEN }}
          NEW: ${{ steps.discover.outputs.latest }}
          OLD: ${{ steps.discover.outputs.current }}
        shell: bash
        run: |
          set -eou pipefail
          branch="ci/bump-kernel-v${NEW}"
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git checkout -B "${branch}"
          git commit -am "Bump CI kernel to v${NEW}"
          git push --force-with-lease origin "${branch}"
          gh pr view "${branch}" >/dev/null 2>&1 || \
            gh pr create \
              --base "${GITHUB_REF_NAME}" \
              --head "${branch}" \
              --title "Bump CI kernel to v${NEW}" \
              --body "Bumps stable kernel pin from \`v${OLD}\` to \`v${NEW}\`."
